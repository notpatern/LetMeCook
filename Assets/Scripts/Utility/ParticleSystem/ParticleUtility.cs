using System.Collections;
using UnityEngine;

namespace ParticleSystemUtility
{
    public static class ParticleUtility
    {
        /*public static void SmoothParticleDestroying(GameObject particle)
        {
            ParticleInstanceManager particleInstance = particle.GetComponent<ParticleInstanceManager>();

            particleInstance.Stop(false);

            particleInstance.StartCoroutine(WaitParticleEndToClear(particleInstance));
        }

        static IEnumerator WaitParticleEndToClear(ParticleInstanceManager particleInstance)
        {
            yield return new WaitForSeconds(particleInstance);
        }*/
    }

}